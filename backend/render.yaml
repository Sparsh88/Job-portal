# DEPRECATED: The active Render Blueprint config is now located at the root of the repository: /render.yaml
# Render requires the render.yaml file to be at the repository root to detect it as a Blueprint.
